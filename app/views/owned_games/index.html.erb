<% if !@owned_games %>
  <h2>You have no games in your library!<h2>
<% else %>
  <ul>
    <% @owned_games.each do |og| %>
      <li>
        <%= link_to og.game.title, game_path(og.game) %>
        -
        <%= og.platform.name %>
      </li>
    <% end %>
  </ul>
<% end %>
