<% if !@owned_games %>
  <h2>You have no games in your library!<h2>
<% else %>
  <ul>
    <% @owned_games.each do |og| %>
      <li>
        <%= link_to og.games_platform.game.title, game_path(og.games_platform.game) %>
        -
        <%= og.games_platform.platform.name %>
      </li>
    <% end %>
  </ul>
<% end %>
